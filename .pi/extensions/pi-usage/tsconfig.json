{
  "//typeRoots": "There is no node_modules beside a .pi/ extension, so @types resolves from the server workspace that owns the toolchain.",
  "//": "Standalone: this extension is loaded by pi's module loader, not built by the makit server, so it has its own strict typecheck (`pnpm typecheck` in server/ runs it too). No rootDir/outDir — nothing is emitted.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2022"],
    "types": ["node"],
    "typeRoots": ["../../../server/node_modules/@types"],
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noImplicitReturns": true
  },
  "include": ["**/*.ts"]
}
