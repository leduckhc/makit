import 'package:flutter/widgets.dart';

import 'coming_soon.dart';

/// Server & Devices section body.
///
/// Placeholder for Wave 1 — Wave 2 fleshes this out in place (see the migration
/// map in SPEC-13). The registry references this widget; do not edit the
/// aggregator to change this body.
class ServerDevicesSection extends StatelessWidget {
  /// Creates the Server & Devices section body.
  const ServerDevicesSection({super.key});

  @override
  Widget build(BuildContext context) => const ComingSoon(title: 'Server & Devices');
}
