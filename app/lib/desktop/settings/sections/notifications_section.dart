import 'package:flutter/widgets.dart';

import 'coming_soon.dart';

/// Notifications section body.
///
/// Placeholder for Wave 1 — Wave 2 fleshes this out in place (see the migration
/// map in SPEC-13). The registry references this widget; do not edit the
/// aggregator to change this body.
class NotificationsSection extends StatelessWidget {
  /// Creates the Notifications section body.
  const NotificationsSection({super.key});

  @override
  Widget build(BuildContext context) => const ComingSoon(title: 'Notifications');
}
