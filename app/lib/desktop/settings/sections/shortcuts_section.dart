import 'package:flutter/widgets.dart';

import 'coming_soon.dart';

/// Shortcuts section body.
///
/// Placeholder for Wave 1 — Wave 2 fleshes this out in place (see the migration
/// map in SPEC-13). The registry references this widget; do not edit the
/// aggregator to change this body.
class ShortcutsSection extends StatelessWidget {
  /// Creates the Shortcuts section body.
  const ShortcutsSection({super.key});

  @override
  Widget build(BuildContext context) => const ComingSoon(title: 'Shortcuts');
}
